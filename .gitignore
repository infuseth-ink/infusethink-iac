# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.lock.hcl
.terraform.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# tfvars may contain sensitive values — keep out of version control
*.tfvars
*.tfvars.json

# dprint cache
.dprint-cache/

# uv / Python tooling (commitizen, pre-commit, dprint-py)
.venv/
__pycache__/
*.pyc

# mise local overrides
.mise.local.toml

# OS
.DS_Store
Thumbs.db

# Individual developer preferences
.vscode/settings.json
